# <ticket-id> — Token-efficient workflow

**Ticket:** <ticket-id> — <short title>  
*(Link to your tracker — e.g. ADO work item URL)*

**Repo:** `C:\GitRepos\<repo-name>` — **not** `<wrong-repo-if-applicable>`

**Prior investigation:** `<scratch_dir>\<ticket-id>-<slug>\investigation.md` (or `none`)

**Related tickets:** <ids and one-line context>

**Model:** `composer-2.5` for execution threads. Opus/thinking only if: <specific escalation trigger>.

**Rule:** One thread per phase. `/diary` between each, then close the tab. <ticket-specific hard stops>.

---

## Thread 1 — <phase name>

**New chat.** Paste:

```
<paste-ready bootstrap prompt — self-contained, paths to workflow.md and investigation, explicit stop condition>
```

**Done when:** <verifiable criterion>

**Notes (for agent, not chat):** <file paths, function names, branch name>

---

## Thread 2 — <phase name>

**New chat.** Paste:

```
<paste-ready bootstrap prompt>
```

**Done when:** <verifiable criterion>

**Fix direction (from ticket):** <one paragraph max>

---

## Thread 3 — <phase name>

**New chat.** Paste:

```
<paste-ready bootstrap prompt — include "say ship it to authorize push/PR">
```

**Done when:** <verifiable criterion>

**After merge:** <post-merge steps — spot-check, tracker close>

---

## Token traps to avoid

| Don't | Do instead |
|---|---|
| | |

---

*Written YYYY-MM-DD — planning thread for token-efficient <ticket-id> workflow.*
